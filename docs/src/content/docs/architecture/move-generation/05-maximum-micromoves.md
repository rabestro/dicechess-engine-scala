---
title: Maximum Micro-moves Rule Algorithm
description: Algorithmic specification for filtering legal moves based on the Dice Chess maximum micro-moves rule.
sidebar:
  order: 5
---

In classic chess, a turn consists of a single move by one player. In **Dice Chess**, a turn consists of a **dice roll** (yielding up to 3 dice results) and a sequence of up to 3 **micro-moves**. 

A player is legally required to maximize their turn's activity by executing the **maximum achievable number of micro-moves** from their rolled dice. This document describes the formal mathematical rules, edge cases, and recursive filtering algorithm used by the engine to enforce this requirement.

---

## The Core Rule

When multiple dice are rolled, a player **must** play a sequence of micro-moves that uses as many of the rolled dice as possible. 
A first move $m_1$ is legally playable if and only if it is part of at least one sequence of moves $(m_1, m_2, \dots, m_k)$ that achieves the absolute maximum number of sequential micro-moves possible from the initial position.

### King-Capture Exemption (Win Condition)
Since capturing the opponent's king is the ultimate win condition, **any move that directly captures the opponent's king is immediately legal**, even if it terminates the turn early and does not achieve the maximum possible micro-move count. Capturing the king wins the game on the spot.

---

## Mathematical Formulation

Let:
* $S$ be the current `GameState`.
* $D$ be the multiset (list) of available dice rolled for the turn (e.g., $D = [1, 4]$ for Pawn and Rook).
* $\text{generate}(S, d)$ be the set of pseudo-legal moves for piece type $d$ in state $S$ (as generated by `MoveGenerator`).
* $S.\text{makeMove}(m)$ be the state resulting from applying move $m$ to state $S$, keeping the active color unchanged.
* $\text{isKingCapture}(S, m)$ be a predicate that returns `true` if $m$ captures the opponent's king in state $S$.

We define the **Maximum Achievable Sequence Length** $L^*(S, D)$ recursively:

$$
L^*(S, D) = \begin{cases}
0 & \text{if } D = \emptyset \\
\max \Big(\{0\} \cup \text{WinningMoves}(S, D) \cup \text{ContinuingMoves}(S, D)\Big) & \text{otherwise}
\end{cases}
$$

Where:
* $\text{WinningMoves}(S, D) = \{ 1 \mid d \in D, m \in \text{generate}(S, d), \text{isKingCapture}(S, m) \}$
* $\text{ContinuingMoves}(S, D) = \{ 1 + L^*(S', D \setminus [d]) \mid d \in D, m \in \text{generate}(S, d), \neg\text{isKingCapture}(S, m) \}$ where $S' = S.\text{makeMove}(m).\text{copy}(\text{activeColor} = S.\text{activeColor})$ and $D \setminus [d]$ removes exactly one instance of $d$ from the multiset $D$.

### Move Legality Criterion
A first move $m_1$ generated by die $d_1 \in D$ in state $S_0$ is **legal** if and only if:
1. $\text{isKingCapture}(S_0, m_1) = \text{true}$, **OR**
2. $1 + L^*(S_1, D \setminus [d_1]) = L^*(S_0, D)$ where $S_1 = S_0.\text{makeMove}(m_1).\text{copy}(\text{activeColor} = S_0.\text{activeColor})$.

---

## Detailed Edge Cases & Exceptions

### 1. King Capture Exemption
If a move $m$ captures the opponent's king, it is instantly legal. The game terminates. We do not evaluate subsequent moves for the remaining dice in that path.

### 2. Multi-Move Piece Relocation
A single physical piece can be moved multiple times in the same turn if the player has multiple appropriate dice. E.g., if a player rolls `[2, 2]` (Knight, Knight), they can move the same Knight twice or two different Knights.

### 3. Pawn Promotions
If a pawn promotes to a Queen on the first micro-move (using a Pawn die `1`), and the remaining dice contain a Queen die `5`, the newly promoted Queen is present on the board and can be moved on the subsequent micro-move.

### 4. Castling
Castling counts as a King move (using King die `6`). It relocates both the King and the Rook. In the subsequent micro-moves of that turn, the castled Rook can still be moved if a Rook die `4` is available and has not been used.

### 5. Intermediate Checks & King Safety
Unlike classic chess, a player is allowed to move their king through checked squares or leave their king in check during the intermediate micro-moves of their turn. The only constraint is that the king cannot be captured by the opponent. If a king is left in check at the end of the turn, the opponent will have the opportunity to capture it on their turn if they roll the correct die.

---

## Step-by-Step Worked Examples

### Example 1: Starting Position with `[1, 4]` (Pawn, Rook)
* **Initial State ($S_0$):** Starting board. All 8 white pawns are on the 2nd rank. White rooks are on $a1$ and $h1$, completely blocked.
* **Available Dice ($D$):** `[1, 4]` (Pawn, Rook).
* **Evaluation:**
  * **Path A (Pawn `e2-e3`):**
    * $S_1 = S_0.\text{makeMove}(\text{e2-e3})$. Rooks are still blocked by $a2, h2, b1, g1$.
    * Generating Rook moves in $S_1$ yields $\emptyset$.
    * $L^*(S_1, [4]) = 0$. Total sequence length = $1 + 0 = 1$.
  * **Path B (Pawn `a2-a3`):**
    * $S_1 = S_0.\text{makeMove}(\text{a2-a3})$. The path for the $a1$ rook is now open.
    * Generating Rook moves in $S_1$ yields `[a1-a2]`.
    * $L^*(S_1, [4]) = 1$. Total sequence length = $1 + 1 = 2$.
  * **Conclusion:** $L^*(S_0, [1, 4]) = 2$. Move `e2-e3` is **illegal** (length 1 < 2). Move `a2-a3` is **legal** (length 2 = 2).

### Example 2: Pawn Promotion with `[1, 5]` (Pawn, Queen)
* **Initial State ($S_0$):** White pawn on $e7$. White has no queens.
* **Available Dice ($D$):** `[1, 5]` (Pawn, Queen).
* **Evaluation:**
  * Since there are no queens on the board, playing Queen first is impossible ($\text{generate}(S_0, 5) = \emptyset$).
  * Playing Pawn first: `e7-e8=Q` promotes the pawn to a Queen.
  * In the resulting state $S_1$, a Queen is now on $e8$.
  * Generating Queen moves in $S_1$ yields multiple legal options (e.g. `e8-e7`, `e8-f7`).
  * $L^*(S_1, [5]) = 1$. Total sequence length = $1 + 1 = 2$.
  * **Conclusion:** The pawn promotion move `e7-e8=Q` is **legal** because it allows a subsequent Queen move, maximizing the sequence length to 2.

### Example 3: King Capture with `[2, 3]` (Knight, Bishop)
* **Initial State ($S_0$):** White Knight can capture the Black King on $f7$. White Bishop is completely trapped behind friendly pawns and has no moves.
* **Available Dice ($D$):** `[2, 3]` (Knight, Bishop).
* **Evaluation:**
  * The Knight capture `Nxf7#` (capturing the King) is a winning move.
  * Even though the Bishop is trapped and $L^*(S_1, [3]) = 0$, the Knight capture is immediately **legal** due to the King-Capture Exemption.

---

## Pseudocode Implementation

### Helper: `canContinueAfterMove`
Recursively checks if there exists a sequence of moves using the remaining dice that achieves the targeted remaining length.

```scala
function canContinueAfterMove(state: GameState, remainingDice: List[Int], targetLength: Int): Boolean = {
  if (targetLength <= 0) {
    return true
  }
  
  for (die <- remainingDice) {
    val moves = MoveGenerator.generateMoves(state, die)
    val remainingAfterDie = remainingDice.removeFirstOccurrence(die)
    
    for (m <- moves) {
      if (isKingCapture(state, m)) {
        // King capture immediately succeeds and satisfies any length
        return true
      }
      
      val nextState = state.makeMove(m).copy(activeColor = state.activeColor)
      if (canContinueAfterMove(nextState, remainingAfterDie, targetLength - 1)) {
        return true
      }
    }
  }
  
  return false
}
```

### Main Filter: `filterMaximalMoves`
Determines the maximum achievable turn length and filters the initial set of moves.

```scala
function filterMaximalMoves(state: GameState, dice: List[Int]): List[Move] = {
  if (dice.isEmpty) {
    return Nil
  }

  // Step 1: Generate all possible first moves across all available dice
  val firstMoves = dice.flatMap(d => MoveGenerator.generateMoves(state, d))
  
  // Step 2: Identify any immediate king captures (instantly legal)
  val kingCaptures = firstMoves.filter(m => isKingCapture(state, m))
  
  // Step 3: Determine the maximum possible sequence length for non-capture paths
  var maxSequenceLength = 1
  val moveLengths = Map[Move, Int]()
  
  for (m <- firstMoves) {
    if (isKingCapture(state, m)) {
      moveLengths(m) = 1
    } else {
      val d = getDieForMove(state, m, dice)
      val remainingDice = dice.removeFirstOccurrence(d)
      val nextState = state.makeMove(m).copy(activeColor = state.activeColor)
      
      // Determine the maximum length reachable starting with this move
      var length = 1
      for (k <- 1 until dice.length) {
        if (canContinueAfterMove(nextState, remainingDice, k)) {
          length = 1 + k
        }
      }
      moveLengths(m) = length
      if (length > maxSequenceLength) {
        maxSequenceLength = length
      }
    }
  }
  
  // Step 4: Filter moves that achieve the maximum sequence length (or are king captures)
  val legalMoves = firstMoves.filter { m =>
    isKingCapture(state, m) || moveLengths(m) == maxSequenceLength
  }
  
  return legalMoves
}
```

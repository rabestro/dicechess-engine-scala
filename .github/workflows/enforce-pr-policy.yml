name: Enforce PR policy

on:
  pull_request:
    types: [opened, edited, synchronize]

jobs:
  check-pr:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate branch name and linked issue
        uses: actions/github-script@v7
        with:
          script: |
            const headRef = context.payload.pull_request.head.ref;
            const body = context.payload.pull_request.body || "";
            const pattern = /^(task|feat|bug)\/\d+-[a-z0-9-]+$/;
            if (!pattern.test(headRef)) {
              core.setFailed(`Branch name "${headRef}" does not match required pattern: (task|feat|bug)/<ID>-<short-desc>`);
              return;
            }
            const issuePattern = /(Closes|Resolves)\s+#\d+/i;
            if (!issuePattern.test(body)) {
              core.setFailed('PR body must include "Closes #<issue-number>" or "Resolves #<issue-number>" linking to the issue.');
              return;
            }


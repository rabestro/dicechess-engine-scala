*.class
*.tasty
*.log

# virtual machine crash logs, see http://www.java.com/en/download/help/error_hotspot.xml
hs_err_pid*
/.bsp/sbt.json

# Scala/SBT
target/
project/target/
project/project/
.history/
.scala-build/
# Synthetic base directories sbt 2 generates for projectMatrix rows (rootJVM/rootJS).
# Currently only ever empty — and git does not track empty directories — so this is
# defensive: it keeps generated paths out of the index if anything starts writing there.
.sbt/

# IDEs
.idea/
*.iml
.vscode/
.DS_Store
dist/

# Local opening book(s) for the arena/eval — never commit book data to this public repo
opening_book.json
*.book.json
dist-wasm/
.wasm/
node_modules/
